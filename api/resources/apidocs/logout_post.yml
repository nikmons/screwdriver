Endpoint to Logout
---
tags:
  - User
securityDefinitions:
  basicAuth:
    type: basic
security:
  - basicAuth: []
parameters:
  - in: "body"
    name: "body"
    description: "Request to Logout"
    required: true
    schema:
      $ref: "#/definitions/Logout"
responses:
  200:
    description: Logout Succesfull
definitions:
  Logout:
    type: "object"
    properties:
      Username:
        type: "string"