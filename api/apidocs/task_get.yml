Endpoint returning a single task
---
tags:
  - Tasks
securityDefinitions:
  basicAuth:
    type: basic
security:
  - basicAuth: []
parameters:
  - name: id
    in: path
    type: integer
    required: true
    description: The id of the task to retrieve
definitions:
  Tasks:
    type: object
    properties:
      tasks:
        type: array
        items:
          $ref: '#/definitions/Task'
  Task:
    type: int
responses:
  200:
    description: A single Task
    schema:
      $ref: '#/definitions/Tasks'